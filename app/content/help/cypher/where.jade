article.help
  section.summary
    h1 Cypher -
      code WHERE
    p
      | The
      code WHERE
      |  clause imposes conditions on the data within a potentially matching path,
      | filtering the result set of a
      code MATCH
      | .
    p
      code MATCH
      |  descibes the structure, and
      code WHERE
      | specfies the content of a query.
  section.example
    h1 Example
    figure
      pre.code(mode='cypher').
        MATCH (director:Person)-[:DIRECTED]->(movie)
        WHERE director.name = "Steven Spielberg"
        RETURN movie.title
      figcaption Find all the many fine films directed by Steven Spielberg.
  section.reference
    h1 Reference
    dl
      dt Details
      dd
        a(href='http://docs.neo4j.org/chunked/{{neo4jVersion}}/query-where.html')
          code WHERE
        |  manual page
      dt Related
      dd
        a(href='/content/help/cypher.html') Cypher overview
        | ,
        a(href='/content/help/match.html')
          code MATCH
