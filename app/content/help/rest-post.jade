article.help
  .container
    section.row-fluid.summary
      .summary.span3
        h3 REST POST
        p.lead
      .details.span9
        p Use 
          code :POST
          |  to send HTTP POST to Neo4j's REST interface.
        table.table-condensed.table-help
          tr
            th Reference:
            td
              a(href='http://docs.neo4j.org/chunked/{{neo4j.version}}/rest-api.html')
                code.icon-external-link-sign &nbsp;REST
          tr
            th Related:
            td
              a(help-topic='rest-get') :help REST GET
              a(help-topic='rest-post') :help REST POST
              a(help-topic='rest-put') :help REST PUT
    section.row-fluid.example
      .span9.offset3
        figure
          pre.code(mode='rest').
            :POST /db/data/node { "name":"Tiberius" }
          figcaption Create a new node, with a name property.
