article.help
  .container
    section.row-fluid.summary
      .summary.span3
        h3 WHERE
        p.lead filter results
      .details.span9
        p
          | The
          code WHERE
          |  clause imposes conditions on the data within a potentially matching path,
          | filtering the result set of a
          code MATCH
          | .
        p
          code MATCH
          |  descibes the structure, and
          code WHERE
          | specfies the content of a query.
        table.table-condensed.table-help
          tr
            th Reference:
            td
              code
                a(href='http://docs.neo4j.org/chunked/{{neo4j.version}}/query-where.html') WHERE
              |  manual page
          tr
            th Related:
            td
              a(help-topic='match') :help MATCH
              | &nbsp;
              a(help-topic='return') :help RETURN
              | &nbsp;
              a(help-topic='cypher') :help Cypher
    section.row-fluid.example
      .span9.offset3
        figure
          pre.code(mode='cypher').
            MATCH (director:Person)-[:DIRECTED]->(movie)
            WHERE director.name = "Steven Spielberg"
            RETURN movie.title
          figcaption Find all the many fine films directed by Steven Spielberg.

