.legend(ng-controller='LegendCtrl')
  ul.unstyled
    li(ng-repeat='rule in rules | filter:isNode')
      a.preview(style='background-color: {{rule.props.fill}};')
      .contents.no-condition
        .class-name {{rule.selector.klass || 'Default'}}
        .condition
      a.remove.btn.btn-small(tooltip-placement='top', tooltip='Remove style', ng-click="remove(rule)")
        i.icon-remove
